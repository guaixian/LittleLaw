name: littlelaw_core
description: LittleLaw LAN peer-to-peer secure communication engine (discovery, pairing, E2E sync, file transfer).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  basic_utils: ^5.8.2
  convert: ^3.1.2
  crypto: ^3.0.7
  cryptography: ^2.9.0
  grpc: ^5.1.0
  protobuf: ^6.0.0
  sqlite3: ^3.5.2
  uuid: ^4.6.0

dev_dependencies:
  test: ^1.26.0
